server:
  port: 8082

spring:
  application:
    name: akhs-discord
  config:
    # Check <your-name> and <your-password> in application.yaml in server config
    import: configserver:http://<your-name>:<your-password>@localhost:8888